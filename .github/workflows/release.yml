name: Build & Release

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write # required to create releases / upload assets

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 11.0.x

      - name: Restore
        run: dotnet restore wincred/wincred.csproj

      - name: Build
        run: dotnet build wincred/wincred.csproj --no-restore -c Release

      - name: Test
        run: dotnet test wincred/wincred.csproj --no-build -c Release --verbosity normal

      - name: Pack
        run: dotnet pack wincred/wincred.csproj --no-build -c Release -o artifacts

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages
          path: |
            artifacts/*.nupkg
            artifacts/*.snupkg
          if-no-files-found: error

      # Everything below only runs for pushes to main - other branches/PRs stop here.
      - name: Is main push?
        id: ismain
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: echo "yes=true" >> "$GITHUB_OUTPUT"

      # The .csproj <Version> is the single source of truth: it's what the
      # package is stamped with, and it's what the release tag tracks.
      - name: Read version
        id: ver
        if: steps.ismain.outputs.yes == 'true'
        shell: pwsh
        run: |
          [xml]$p = Get-Content wincred/wincred.csproj
          $v = ($p.Project.PropertyGroup.Version | Where-Object { $_ }) | Select-Object -First 1
          if (-not $v) { throw "No <Version> found in the .csproj." }
          $v = "$v".Trim()
          "version=$v" >> $env:GITHUB_OUTPUT
          "tag=v$v" >> $env:GITHUB_OUTPUT
          Write-Host "Version: $v -> tag v$v"

      # A release is cut only when <Version> is bumped to something not yet tagged.
      # Pushes that don't change the version build + upload the artifact but skip
      # the release (re-tagging an existing version would clobber the release).
      - name: Check whether tag already exists
        id: tagcheck
        if: steps.ismain.outputs.yes == 'true'
        shell: pwsh
        run: |
          git fetch --tags --quiet
          $exists = (git tag --list "${{ steps.ver.outputs.tag }}")
          if ($exists) {
            Write-Host "Tag ${{ steps.ver.outputs.tag }} already exists - skipping release."
            "exists=true" >> $env:GITHUB_OUTPUT
          } else {
            "exists=false" >> $env:GITHUB_OUTPUT
          }

      - name: Push to NuGet
        if: steps.ismain.outputs.yes == 'true' && steps.tagcheck.outputs.exists == 'false'
        run: dotnet nuget push "artifacts/*.nupkg" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Create Release
        if: steps.ismain.outputs.yes == 'true' && steps.tagcheck.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: ${{ steps.ver.outputs.tag }}
          generate_release_notes: true
          files: |
            artifacts/*.nupkg
            artifacts/*.snupkg
